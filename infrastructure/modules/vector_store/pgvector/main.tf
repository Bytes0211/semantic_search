terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0"
    }
  }
}

# Phase 4 stub — pgvector/RDS vector store infrastructure is not yet implemented.
