terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0"
    }
  }
}

# Phase 3 stub — SageMaker-hosted embedding infrastructure is not yet implemented.
